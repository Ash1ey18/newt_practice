import styled from "styled-components";
import Image from "next/image";

export const StyledUl = styled.ul`
  list-style: none;
  padding-inline-start: 0 !important;
  margin: 12px 20px 0 20px;
`;

export const StyedImage = styled(Image);
